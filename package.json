{
  "name": "wartremover",
  "version": "2.1.1",
  "description": "stream transform to turn bunyan json log entries into plaintext",
  "keywords": [
    "bunyan"
  ],
  "author": "Robey Pointer <robeypointer@gmail.com>",
  "homepage": "https://github.com/robey/node-wartremover",
  "repository": {
    "type": "git",
    "url": "git://github.com/robey/node-wartremover.git"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "clicolor": "^2.2.2"
  },
  "devDependencies": {
    "bunyan": "^1.8.1",
    "eslint": "^1.4.1",
    "mocha": "^3.0.0",
    "should": "^10.0.0"
  },
  "main": "./src/wartremover",
  "files": [
    "src"
  ],
  "engines": {
    "node": ">=6.3.0"
  },
  "scripts": {
    "build": "eslint src test/src",
    "distclean": "rm -rf node_modules npm_debug.log",
    "prepublish": "npm run build",
    "test": "npm run build && mocha -R spec --colors test/src"
  }
}
